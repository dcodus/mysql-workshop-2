1. select contentLineOne from Entry where contentLineOne like '(3%' or contentLineOne like '1-3%';
We used the simple matching for (3% and 1-3% to find all instances.
60

2. select * from Entry where type ='home' and subtype ='phone' and contentLineOne not like '%5%';  
76
3. select * from Entry where type = 'work' and subtype = 'phone' and contentLineOne like '1-%';
60
